# Copy to .env to change the ports used by start.sh (env vars take precedence).
#BACKEND_PORT=8000
#FRONTEND_PORT=5173

# Optional Claude cloud chat: exported by start.sh so the backend picks it up
# (can also live in backend/.env).
#INBOX_ANTHROPIC_API_KEY=
