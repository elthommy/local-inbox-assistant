# Copy to backend/.env and adjust. All variables are optional; defaults shown.
#
# This file is the only .env the backend reads. The repo-root .env is for
# start.sh (ports) and is NOT a place to put INBOX_* variables — apart from
# INBOX_ANTHROPIC_API_KEY, which start.sh exports explicitly, they are
# silently ignored there. Changes here take effect on backend restart.

# Root scanned recursively for .eml files: every mailbox folder under it is
# indexed. Point it at a single folder's cur/ dir to index only that folder.
# Default: auto-detected Thunderbird profile (newest *.default-release or
# *.default under ~/.thunderbird) — Linux only, so macOS
# (~/Library/Thunderbird/Profiles) and Windows (%APPDATA%\Thunderbird\Profiles)
# must set this by hand. When detection finds nothing, indexing fails with
# "maildir root not found: ..." and this is the setting to fix.
# Only maildir-style storage (one .eml file per message) is supported;
# Thunderbird's default mbox format is not — see the README.
#INBOX_MAILDIR=~/.thunderbird/xxxxxxxx.default-release

# Folder names skipped during the scan (comma-separated, case-insensitive;
# also skips their subfolders). "All Mail" is Gmail's duplicate label archive.
#INBOX_EXCLUDE_FOLDERS=Trash,Junk,Spam,Drafts,Unsent Messages,All Mail

# Indexing window (days of mail kept in the index)
#INBOX_WINDOW_DAYS=90

# LLM task/event/priority extraction only runs on mail newer than this
#INBOX_EXTRACTION_WINDOW_DAYS=14
#INBOX_EXTRACTION_MAX_EMAILS=300

# Ollama. The chat and email parsing models are chosen in the UI (settings
# drawer -> Models), not here; see "Changing models" in the README.
#INBOX_OLLAMA_URL=http://localhost:11434
#INBOX_EMBED_MODEL=nomic-embed-text

# Claude cloud chat (opt-in). With a key set, "Claude (cloud)" becomes
# selectable in the chat dropdown; indexing/extraction/embeddings stay local.
#INBOX_ANTHROPIC_API_KEY=
#INBOX_CLAUDE_MODEL=claude-opus-5
